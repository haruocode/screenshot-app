{
  "name": "screenshot-app",
  "version": "1.0.0",
  "description": "An efficient app to capture left and right pages from ebooks.",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && electron ."
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "electron": "^28.0.0",
    "screenshot-desktop": "^1.15.0",
    "sharp": "^0.33.0"
  }
}
